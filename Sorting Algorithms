#Bubble Sort
def bubblesort(num):
    do = True
    while do:
        do = False
        for i in range(len(num)-1):
            if num[i] > num[i+1]:
                num[i], num[i+1] = num[i+1], num[i]
                do = True
